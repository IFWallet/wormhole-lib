const airdrop = require('./src/airdrop')
const burn = require('./src/burn')
const common = require('./src/common')
const crowsale = require('./src/crowsale')
const fixed = require('./src/fixed')
const managed = require('./src/managed')
const transfer = require('./src/transfer')

module.exports = {
  airdrop,
  burn,
  common,
  crowsale,
  fixed,
  managed,
  transfer
}